name: Security (reusable)

on:
  workflow_call:
    inputs:
      has_go:
        type: boolean
        default: true

jobs:
  debug:
    name: Debug check
    runs-on: ubuntu-latest
    steps:
      - run: echo "Reusable workflow loaded OK. has_go=${{ inputs.has_go }}"
